---
// BlogCard.astro
const { title, excerpt, link } = Astro.props;
---

<div class="rounded-xl border p-6 shadow-md hover:shadow-lg transition">
    <h3 class="text-xl font-semibold mb-2">{title}</h3>
    <p class="text-gray-600 mb-4">{excerpt}</p>
    <a href={link} class="text-blue-500 hover:underline">阅读更多</a>
</div>
